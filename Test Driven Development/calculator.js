/**
 * Function adds two arguments that contain / return numbers of any type 
 * @param {} x 
 * @param {*} y 
 */

/**
 * Write an addition function for following tests
 * 1. Two numbers
 * 2. Two number arrays
 * 3. Two functions that return numbers
 * @param {} x 
 * @param {*} y 
 */

function add(x, y) {
    return x + y;
}

function addTwoArrays([array1], [array2]) {
    return array1 + array2; //add the number in two arrays and return the sum
}

function addNumbersFromTwoFunctions(number1, number2){
    return number1 + number2;
}

function subtract(a, b){
    return a - b;
}
