/**Problem Statement
 * 1. Function with 2 arguements
 * 2. The 2 arguements should be arrays of numbers
 * 3. Return one output. The output is an array of numbers.
 *  - Sum of the numbers provided in the input arrays.
 * 
 * Input
 * Argument 1- Array (1,2) / single dimensional array
 * Arguement 2- Array (2,3)
 * 
 */

//var array1 = [1,2]
//var array2 = [2,3]

//function sumOfArray (array1, array2){
       /**print the numbers of array1*/

//var array3 = new Array();
  //     for(counter=0; counter<=1; counter++) {
    //Add array1[0] + array2[0] = array3[0]
    //   array3[counter] = array1[counter] + array2[counter];
      // }
      // console.log(array3);

    //return array3; //sum of array1 and array2 : [3,5]
//}


/**More functions that an array supports
 * reverse, join, slice
 */

var array = [1,2,3,4,5,6];
console.log('Reverse of the array -> ' + array.reverse());
console.log